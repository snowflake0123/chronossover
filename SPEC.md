# Chronossover 仕様書

## 1. アプリケーション概要

**Chronossover** は、ユーザーが指定した「過去または未来の西暦」において、もし当時の日本に Twitter ライクな SNS が存在していたとしたら人々はどのような発言・やり取りをしていただろうか、という空想を AI（生成 AI）に想像させ、あたかもその時代の SNS のタイムラインを閲覧・参加しているかのような体験を提供するシングルページ Web アプリケーションである。

ユーザーは単なる閲覧者ではなく、個別の投稿に対して自ら返信を書き込むこともでき、その返信に対して元投稿者（AI が演じる当時の人物）がさらに返信してくる、という時間を超えた擬似的な交流が体験の中心になる。

## 2. 利用シーン・想定ユーザー

- 過去の流行を振り返って楽しみたいユーザー
- 未来を空想してみたいユーザー
- 生成 AI の API 利用料を自分で負担してもかまわないユーザー（本アプリは開発利用・個人利用を前提とし、API キーはビルド時の環境変数としてバンドルに含める）

## 3. 用語定義

| 用語             | 定義                                                                                   |
| ---------------- | -------------------------------------------------------------------------------------- |
| 対象年           | ユーザーが「その年のタイムラインを見たい」として指定する西暦年                         |
| トレンド         | 対象年の日本で流行している（していた／していると想像される）事物の箇条書きリスト       |
| タイムライン     | 対象年に投稿されたという体裁の、複数の投稿の一覧                                       |
| 投稿             | 仮想ユーザー（対象年の日本に住む一般人）が書いたとされる一件の SNS メッセージ          |
| 返信             | ある投稿に紐づくメッセージ。ユーザー自身の返信と、元投稿者（AI）からの返信の両方を含む |
| ユーザー（末端） | アプリを操作する現実の利用者                                                           |
| 仮想ユーザー     | タイムライン上に現れる、対象年に生きているとされる架空の人物                           |

## 4. 画面構成と遷移

アプリは以下の 3 つの主要画面で構成される。

```
[スタート画面] ──Chronossover ボタン──▶ [ホーム画面（タイムライン）] ──投稿タップ──▶ [投稿詳細画面]
                                                                                       ▲
                                                                                       │
                                                                                    戻るボタンでホームへ
```

URL パスによる画面対応:

- `/` — スタート画面
- `/home` — ホーム画面
- `/message/:id` — 投稿詳細画面（`:id` は該当投稿の識別子）

### 4.1 スタート画面

**目的**: 対象年を受け取り、タイムライン生成をトリガーする。生成 AI の API キーは環境変数（`VITE_OPENAI_API_KEY`）から自動で読み込むため、ユーザーに入力させない。

**画面要素**:

- 対象年の選択 UI
  - 年のみを選択できる（月日は選ばない）
  - 選択可能範囲は 0 年〜9999 年（範囲外の値は入力時点でクランプする）
- 実行ボタン（ラベル「Chronossover!」）
- トレンド表示エリア
  - 生成完了までは「ここにトレンドが表示される予定です」と表示
  - 生成後はトレンド文字列を表示
- 処理中のローディングインジケーター（不確定プログレスバー）

**振る舞い**:

1. 実行ボタン押下時、対象年が未入力なら何もしない。
2. 対象年が入力されていれば、
   1. 環境変数 `VITE_OPENAI_API_KEY` から API キーを取得する。未設定の場合は、その旨をダイアログで通知して処理を中止する。
   2. 現在年と対象年を比較し、対象年が現在年以前なら「過去モード」、それ以後なら「未来モード」として動作する。
   3. 生成 AI にトレンドを問い合わせる（モードにより指示文が切り替わる）。
   4. 得られたトレンドを画面に表示し、かつ次のステップの入力として保持する。
   5. そのトレンドを踏まえて、当時／未来の SNS 投稿群を生成 AI に問い合わせる。
   6. 得られた投稿群をパースしてアプリの状態に保持する。
   7. ホーム画面へ遷移する。
3. いずれかの API 呼び出しに失敗した場合、失敗した段階に応じたエラーメッセージをダイアログで表示し、ホーム画面へは遷移しない。
4. API 呼び出しは指数バックオフによるリトライを行う。

### 4.2 ホーム画面（タイムライン）

**目的**: 生成された投稿群を SNS のタイムラインとして表示する。

**画面要素**:

- ヘッダータイトル: 「Timeline in {対象年}」
- 投稿一覧（リスト形式）
  - 各行に以下を表示
    - アイコン（共通の人物アイコン）
    - 仮想ユーザーの表示名
    - 仮想ユーザーの ID（`@` プレフィックス付き）
    - 投稿日時（「YYYY年M月D日」形式）
    - 本文（ハッシュタグ含むテキスト）
- プルリフレッシュ UI（スクロール位置が先頭のときにリストを下に引っ張ると、追加の投稿を取得する）

**振る舞い**:

- 各投稿行タップで投稿詳細画面へ遷移する。
- 初回の投稿一覧はスタート画面で生成した結果を表示する。
- プルリフレッシュ時は、スタート画面で取得済みのトレンドを再利用して生成 AI に投稿群を再問い合わせし、得られた投稿を既存一覧の先頭に追加する。
  - 追加後も、ユーザーが引っ張った時点で見ていた位置（＝既存一覧の先頭）を視覚的に保持するため、追加で増加したコンテンツ高さぶんだけ `scrollTop` を加算して相殺する。
  - 追加取得中はローディングインジケーター（ドットスピナー）をリスト上部に表示する。
  - 取得失敗時はダイアログで通知し、既存一覧はそのまま維持する。

### 4.3 投稿詳細画面

**目的**: 選択した投稿の全文表示、および仮想ユーザーとの対話（返信）を行う。

**画面要素**:

- 戻るボタン（ラベル「Timeline in {対象年}」、戻り先はホーム画面）
- 元投稿の表示
  - アイコン／表示名／ユーザーID／本文／日時（日時は「HH:MM・YYYY年M月D日」形式）
- 返信一覧（時系列順で下に積み上がる）
  - ユーザー自身の返信と、仮想ユーザーからの返信がここに混在表示される
  - 各返信もアイコン・表示名・ユーザーID・本文を持つ
- 仮想ユーザーからの返信を生成中のローディング表示（ドットスピナー）
- 返信入力エリア
  - プレースホルダー「返信を入力」
  - 複数行入力対応（自動拡張）
  - 「送信」ボタン

**振る舞い**:

1. 画面表示時、URL パラメータの `id` に一致する投稿を現在のタイムラインから検索し、表示する。
2. 一致する投稿が見つからない場合はダイアログで通知しスタート画面へ戻す。
3. 「送信」ボタン押下時、
   1. 入力内容をユーザーからの返信としてすぐに返信一覧へ追加する。
   2. 入力欄をクリアする。
   3. 一定の待機後にローディング表示を開始する。
   4. 元投稿・これまでの全返信・対象年を踏まえて、生成 AI に「元投稿者（仮想ユーザー）としての返信」を問い合わせる。
   5. 得られた返信を仮想ユーザーの名義で返信一覧に追加する。
   6. 失敗時はエラーメッセージをダイアログ表示する。
4. 返信は永続化されない（画面を離れると失われる前提。実装上の都合）。

## 5. ドメインモデル

### 5.1 投稿（Message）

| フィールド | 型                | 説明                                 |
| ---------- | ----------------- | ------------------------------------ |
| id         | 文字列（一意）    | 投稿の識別子                         |
| user.id    | 文字列            | 仮想ユーザーの ID（`@` なし）        |
| user.name  | 文字列            | 仮想ユーザーの表示名                 |
| date       | 文字列（ISO8601） | 投稿日時                             |
| body       | 文字列            | 本文（ハッシュタグ等を含む自由記述） |

返信も同じモデルを共有する。ユーザー自身の返信は `user.id = "you"`, `user.name = "You"` という固定値を使う。

### 5.2 アプリケーション状態（グローバル）

- 対象年
- タイムラインの投稿配列

これらは起動中のみ保持され、永続化されない。リロードで初期化される。生成 AI の API キーはアプリケーション状態ではなく、ビルド時に注入される環境変数として扱う。

## 6. 外部連携

### 6.1 生成 AI（チャット補完 API）

本アプリは外部の生成 AI のチャット補完 API を利用する。認証は、ビルド時に注入される環境変数 `VITE_OPENAI_API_KEY` に設定した API キーで行う。キーはバンドルに含まれてクライアント側から参照可能になるため、**開発利用・個人利用のみを想定**し、第三者に公開する形での本番デプロイは行わない。

本アプリから呼び出すタスクは次の 3 種類。

#### (a) トレンド取得

- 入力: 対象年、モード（過去／未来）
- 指示（過去モード）:
  - 対象年の日本で実際に流行したものを多分野にわたって箇条書きで列挙させる
  - 正確性を優先し、前置きや後書きは出力させない
- 指示（未来モード）:
  - 対象年の日本で流行しそうなものを想像で列挙させる
  - 架空の商品名・サービス名を含め、未来感のある命名を求める
  - 同じく前置き・後書きは出力させない
- 出力: 自然文（箇条書きのテキスト）

#### (b) タイムライン取得

- 入力: 対象年、(a) で得られたトレンド
- 指示:
  - 対象年の日本に Twitter ライクな SNS があったと仮定する
  - 多様な性格・口調の一般人の発言を想像させる
  - 発言の長さは 20〜100 文字程度で多様に
  - 発言は日時の降順（新しい順）に並べさせる
  - 指定のトレンドを発言内容に反映させる（複数人が同じトレンドに言及するケースも歓迎）
  - 出力フォーマットを厳格に指定する（下記 §6.2 参照）
- 出力: 区切り文字 `\eot` で終わる 1 件単位のテキストの連結。装飾・番号・前置き・後書きは禁止。

#### (c) 返信取得

- 入力: 対象年、元投稿、これまでの返信群
- 指示:
  - AI に元投稿者の仮想ユーザーを演じさせる
  - 元投稿の口調を保ちつつ多少フランクに
  - 返信本文のみを出力させ、装飾・状況説明は禁止
- 出力: 自然文（返信本文そのもの）

### 6.2 タイムライン取得結果のフォーマット

タイムライン取得（(b)）の出力は、次の形式を 1 件とし、`\eot` で区切って連結したテキストである。

```
@{ユーザーID}, {ユーザー名}, {ISO8601 日時}, {本文}\eot
```

アプリは受信文字列を `\eot` で分割し、空要素を除外、改行を除去した上で、カンマ区切りの 4 要素としてパースする。先頭要素は `@` の最初の出現位置より後ろを ID として取り出すことで、AI が前置きの余計な文字を混ぜた場合にも頑健に動作させる。各投稿にはアプリ側で一意な識別子を付与する。

## 7. エラー処理方針

- 生成 AI への通信失敗は指数バックオフで自動リトライする。最終的に失敗した場合は段階（トレンド取得／タイムライン取得／返信取得）ごとに文言を切り替えたアラートを出す。
- 存在しない投稿 ID が指定された場合は、ユーザーに通知した上でスタート画面に戻す。
- 環境変数 `VITE_OPENAI_API_KEY` が未設定の場合は、環境変数の設定を促す文言をダイアログで表示し、API 呼び出しは行わない。
- 対象年の未入力時、実行ボタンは何も行わない（現状は明示的なバリデーションメッセージは出さない）。

## 8. 非機能・その他

- アプリは Web／モバイルアプリの双方で動作することを想定したハイブリッドアプリ構成（シェル／ネイティブ化対応を含む）。
- 状態はメモリ上のみ。対象年・投稿群・返信はページリロードで失われる。API キーは環境変数経由でビルドに埋め込まれるため、実行中に「失われる」概念の対象外。
- 返信は対象の投稿者（仮想ユーザー）になりすます形で AI が書き、現実のユーザーのなりすまし判別や投稿保存は行わない。
- 利用料は API キー所有者（このアプリをビルド・起動した当人）の負担となる。API キーがバンドルに含まれる性質上、本アプリは開発利用・個人利用のみを想定する。
